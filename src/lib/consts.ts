export const locales = ["en", "de"];

// Limits the output to prevent very long answers
export const maxTokens = 1000;

// Change to your prompt.
export const aiSystemMessage = "You are a helpful assistant.";
